<h1>Create your new clinic - Basic Information</h1>
<%= form_for @shop, html: { class: 'col-md-6 col-md-offset-3' } do |f| %>
  <%= render 'basic', f: f %>
  <div class='form-group'>
    <%= f.submit "Next", class: 'btn btn-success' %>
  </div>
<% end %>